#+title:    noirmacs
#+AUTHOR:   Narmer Noir Bazile
#+EMAIL:    narmernbazile@gmail.com
#+LANGUAGE: en
#+STARTUP:  overview
#+PROPERTY: header-args :tangle yes :results silent
TODO: avy, consult, embark, wgrep?

* early init
#+begin_src emacs-lisp
  (tool-bar-mode -1) ;; All these tools are in the menu-bar anyway
;; Disable tool-bar (GUI buttons)
(when (fboundp 'tool-bar-mode)
  (tool-bar-mode -1))

;; Disable scroll bars
(when (fboundp 'scroll-bar-mode)
  (scroll-bar-mode -1))

;; Disable menu bar
(when (fboundp 'menu-bar-mode)
  (menu-bar-mode -1))

  ;; Default frame configuration
  (setq frame-resize-pixelwise t)

  ;; intelligently set default window geometry
  ;;(defun set-frame-size-according-to-resolution ()
  ;;  (interactive)
  ;;  (if window-system
  ;;  (progn
  ;;    ;; use 120 char wide window for largeish displays
  ;;    ;; and smaller 80 column windows for smaller displays
  ;;    ;; pick whatever numbers make sense for you
  ;;    (if (> (x-display-pixel-width) 1280)
  ;; 	    (add-to-list 'default-frame-alist (cons 'width 120))
  ;;	    (add-to-list 'default-frame-alist (cons 'width 80)))
  ;;    ;; for the height, subtract a couple hundred pixels
  ;;    ;; from the screen height (for panels, menubars and
  ;;    ;; whatnot), then divide by the height of a char to
  ;;    ;; get the height we want
  ;;    (add-to-list 'default-frame-alist 
  ;; 	  (cons 'height (/ (- (x-display-pixel-height) 200)
  ;; 			      (frame-char-height))))))

  ;;(set-frame-size-according-to-resolution)

  ;;(setq default-frame-alist '((fullscreen . maximized)
  ;;
  ;;			     ;; You can turn off scroll bars by uncommenting these lines:
  ;;			     ;; (vertical-scroll-bars . nil)
  ;;			     ;; (horizontal-scroll-bars . nil)
  ;;
  ;;			     ;; Setting the face in here prevents flashes of
  ;;			     ;; color as the theme gets activated
  ;;			     (background-color . "#000000")
  ;;			     (ns-appearance . dark)
  ;;			     (ns-transparent-titlebar . t)))
  ;;

#+end_src

* init
** Package settings
#+begin_src emacs-lisp
  ;; Package setup
  ;;(with-eval-after-load 'package
  ;;(add-to-list 'package-archives '("melpa" . "https://melpa.org/packages/") t))

  ;; configure package sources
  (require 'package)
  (setq package-enable-at-startup nil)
  (setq package-archives '(;;("org"   . "http://orgmode.org/elpa/")
			   ("gnu"   . "http://elpa.gnu.org/packages/")
			   ("melpa" . "https://melpa.org/packages/")))
  (package-initialize)

  ;; bootstrap =use-package=
  (unless (package-installed-p 'use-package)
    (package-refresh-contents)
    (package-install 'use-package))
  (require 'use-package)
#+end_src
** Enviroment settings
#+begin_src emacs-lisp
  (use-package exec-path-from-shell
    :ensure t
    :config
    (exec-path-from-shell-initialize))
#+end_src
** Basic settings
#+begin_src emacs-lisp

  ;; Keep emacs Custom-settings in separate file.
  (let ((custom-path (expand-file-name "custom.el" user-emacs-directory)))
    (unless (file-exists-p custom-path)
      (with-temp-file custom-path))
    (setq custom-file custom-path)
    (load custom-file))


  (setopt initial-major-mode 'fundamental-mode)          ;; default mode for the *scratch* buffer
  (savehist-mode)                                        ;; save history of minibuffer
  (windmove-default-keybindings 'control)                ;; Move through windows with Ctrl-<arrow keys>
  (setopt sentence-end-double-space nil)                 ;; Fix archaic defaults
  (setq make-backup-files nil)                           ;; Disable backup files
  (setq backup-directory-alist  '((".*" . "~/.Trash"))) 

  (setq visible-bell t)                                  ;; disable terminal bell
  (delete-selection-mode 1)                              ;; replace active region by typing
  (defalias 'yes-or-no-p 'y-or-n-p)                      ;; skip typing out yes or no

  (setq locale-coding-system   'utf-8) ;; set UTF-8 encoding as default
  (set-terminal-coding-system  'utf-8)
  (set-keyboard-coding-system  'utf-8)
  (set-selection-coding-system 'utf-8)
  (prefer-coding-system        'utf-8)

#+end_src
** Interface enhancements/defaults
#+begin_src emacs-lisp
  ;; Mode line information
  (setopt line-number-mode t)                           ;; Show current line in modeline
  (setopt column-number-mode t)                         ;; Show column as well

  ;; Enable horizontal scrolling
  (setopt mouse-wheel-tilt-scroll t)
  (setopt mouse-wheel-flip-direction t)

  ;; Display line numbers in programming mode
  (add-hook 'prog-mode-hook 'display-line-numbers-mode)
  (setopt display-line-numbers-width 3)                 ;; Set a minimum width

  ;; Nice line wrapping when working with text
  (add-hook 'text-mode-hook 'visual-line-mode)          ;; enable word-wrapping for text and programming modes
  (setq-default fill-column 80)                         ;; wrap lines at standard 80 characters
  (setq-default truncate-lines nil)                     ;; nil for wrap, t for no wrap

  ;; Modes to highlight the current line with
  (let ((hl-line-hooks '(text-mode-hook prog-mode-hook)))
    (mapc (lambda (hook) (add-hook hook 'hl-line-mode)) hl-line-hooks))

  ;; Misc. UI tweaks
  ;;(blink-cursor-mode -1)                                ; Steady cursor
  (pixel-scroll-precision-mode)                           ; Smooth scrolling

#+end_src
** Text editing settings
#+begin_src emacs-lisp
  ;; auto-close parentheses, brackets, and quotes in programming modes
  (use-package electric
    :hook (prog-mode . electric-pair-mode)
    :init
    (setq electric-pair-pairs '((?\( . ?\))
				(?\[ . ?\])
				(?\{ . ?\})
				(?\" . ?\"))))

  ;; We won't set these, but they're good to know about
  ;; (setopt indent-tabs-mode nil)
  ;; (setopt tab-width 4)
#+end_src
** Icons, themes, and fonts, and dashboard
#+begin_src emacs-lisp

  (use-package doom-themes
    :ensure t
    :config
    (load-theme 'doom-one-light t))

  (use-package all-the-icons
    :ensure t)

;; (use-package dashboard
;;   :ensure t
;;   :after (all-the-icons)
;;   :config
;;   (dashboard-setup-startup-hook))

#+end_src
** Font configuration
#+begin_src emacs-lisp
  (set-face-attribute 'default nil
    :family "JetBrains Mono NL"
    :height 105)   ;; height = 1/10 pt; 120 ≈ 12pt

  ;;(set-face-attribute 'variable-pitch nil
  ;;  :family "IBM Plex Serif"
  ;;  :height 140)

  (set-face-attribute 'fixed-pitch nil
    :family "JetBrains Mono"
    :height 120)
#+end_src
** Discovery aids
#+begin_src emacs-lisp
  (use-package which-key
    :ensure t
    :config
    (which-key-mode))
#+end_src
** Minibuffer/completion settings
#+begin_src emacs-lisp
  ;; For help, see: https://www.masteringemacs.org/article/understanding-minibuffer-completion

  (setopt enable-recursive-minibuffers t)                ; Use the minibuffer whilst in the minibuffer
  (setopt completion-cycle-threshold 1)                  ; TAB cycles candidates
  (setopt completions-detailed t)                        ; Show annotations
  (setopt tab-always-indent 'complete)                   ; When I hit TAB, try to complete, otherwise, indent
  (setopt completion-styles '(basic initials substring)) ; Different styles to match input to candidates

  (setopt completion-auto-help 'always)                  ; Open completion always; `lazy' another option
  (setopt completions-max-height 20)                     ; This is arbitrary
  (setopt completions-detailed t)
  (setopt completions-format 'one-column)
  (setopt completions-group t)
  (setopt completion-auto-select 'second-tab)            ; Much more eager
  ;(setopt completion-auto-select t)                     ; See `C-h v completion-auto-select' for more possible values

  (keymap-set minibuffer-mode-map "TAB" 'minibuffer-complete) ; TAB acts more like how it does in the shell
#+end_src
** Minibuffer/completion packages
#+begin_src emacs-lisp
  ;; Vertico: better vertical completion for minibuffer commands
  (use-package vertico
    :ensure t
    :init
    ;; You'll want to make sure that e.g. fido-mode isn't enabled
    (vertico-mode))

  (use-package vertico-directory
    :after vertico
    :bind (:map vertico-map
                ;; make backspace key delete entire directory
                ("DEL" . vertico-directory-delete-word)))

  ;; Marginalia: annotations for minibuffer
  (use-package marginalia
    :ensure t
    :config
    (marginalia-mode))

  ;; Orderless: powerful completion style
  (use-package orderless
    :ensure t
    :config
    (setq completion-styles '(orderless)))

  ;; Popup completion-at-point
  (use-package corfu
    :ensure t
    :init
    (global-corfu-mode)
    :bind
    (:map corfu-map
          ("SPC" . corfu-insert-separator)
          ("C-n" . corfu-next)
          ("C-p" . corfu-previous))
    :custom
    (corfu-auto t) ;; Enable auto completion
    (corfu-cycle t) ;; Allow cycling through suggestions
    (corfu-preview-current nil) ;; Disable preview of the current candidate
    (corfu-auto-prefix 2) ;; Minimum prefix length for auto completion
    (corfu-auto-delay 0.2)) ;; Delay in seconds before suggestions pop up

  ;; Part of corfu
  (use-package corfu-popupinfo
    :after corfu
    :hook (corfu-mode . corfu-popupinfo-mode)
    :custom
    (corfu-popupinfo-delay '(0.25 . 0.1))
    (corfu-popupinfo-hide nil)
    :config
    (corfu-popupinfo-mode))

  ;; Make corfu popup come up in terminal overlay
  ;; (use-package corfu-terminal
  ;;   :if (not (display-graphic-p))
  ;;   :ensure t
  ;;   :config
  ;;   (corfu-terminal-mode))

  ;; Fancy completion-at-point functions; there's too much in the cape package to
  ;; configure here; dive in when you're comfortable!
  (use-package cape
    :ensure t
    :init
    (add-to-list 'completion-at-point-functions #'cape-dabbrev)
    ;;(add-to-list 'completion-at-point-functions #'cape-symbol) ;; Symbol completion
    (add-to-list 'completion-at-point-functions #'cape-file))

  ;; Pretty icons for corfu
  (use-package kind-icon
    :if (display-graphic-p)
    :ensure t
    :after corfu
    :config
    (add-to-list 'corfu-margin-formatters #'kind-icon-margin-formatter))

#+end_src
** Modeline packages
#+begin_src emacs-lisp
  (use-package doom-modeline
    :ensure t
    :requires all-the-icons
    :config
    (setq doom-modeline-major-mode-color-icon nil)
    (setq doom-modeline-major-mode-icon nil)
    (setq doom-modeline-minor-modes t)
    (doom-modeline-mode 1))

  (use-package diminish
    :ensure t
    :init
    (diminish 'which-key-mode)
    ;; required because flyspell is loaded with a hook rather than at startup
    (with-eval-after-load "flyspell" (diminish 'flyspell-mode))
    (with-eval-after-load "flyspell" (diminish 'flyspell-prog-mode)))

  (use-package minions
    :ensure t
    :config (minions-mode 1))
#+end_src
** Tab-bar configuration
#+begin_src emacs-lisp
  ;; Show the tab-bar as soon as tab-bar functions are invoked
  (setopt tab-bar-show 1)
#+end_src
** Development configuration
*** general (treesitter, eglot)
#+begin_src emacs-lisp
  ;; common file types
  (use-package markdown-mode :hook ((markdown-mode . visual-line-mode)))
  (use-package yaml-mode :ensure t)
  (use-package json-mode :ensure t)


  ;; Treesitter config

  ;; Tell Emacs to prefer the treesitter mode
  ;; You'll want to run the command `M-x treesit-install-language-grammar' before editing.
  (setq major-mode-remap-alist
  	'((yaml-mode . yaml-ts-mode)
  	  (bash-mode . bash-ts-mode)
  	  (js2-mode . js-ts-mode)
  	  (typescript-mode . typescript-ts-mode)
  	  (json-mode . json-ts-mode)
  	  (css-mode . css-ts-mode)
  	  (python-mode . python-ts-mode)))


  ;; Eglot, the built-in LSP client for Emacs

  (use-package eglot
    :hook ((python-ts-mode . eglot-ensure)
           (c-mode . eglot-ensure)
           (c++-mode . eglot-ensure))
    :config
    (add-to-list 'eglot-server-programs '(java-mode . ("jdtls")))
    (add-to-list 'eglot-server-programs '(rustic-mode . ("rust-analyzer")))
    (setq eglot-send-changes-idle-time 0.3)
    (add-hook 'eglot-managed-mode-hook #'eldoc-mode))
#+end_src
** Evil-mode configuration
#+begin_src emacs-lisp
  ;; Evil: vi emulation
  (use-package evil
    :ensure t

    :init
    (setq evil-respect-visual-line-mode t)
    (setq evil-undo-system 'undo-tree)
    (setq undo-tree-auto-save-history nil)
    (setq evil-want-C-i-jump nil) ;; DONT TOUCH THIS: need to fix broken tab functionality
    ;; Enable this if you want C-u to scroll up, more like pure Vim
    ;(setq evil-want-C-u-scroll t)

    ;; Set default state to Emacs state for all modes
    (setq evil-default-state 'emacs)

    :config
    ;;(evil-define-key 'normal org-mode-map (kbd "<tab>") #'org-cycle)
    (evil-mode 1)

    ;; Configuring initial major mode for some modes
    (evil-set-initial-state 'text-mode 'normal)
    (evil-set-initial-state 'prog-mode 'normal)
    (evil-set-initial-state 'toml-mode 'normal)
    (evil-set-initial-state 'json-ts-mode 'normal)
    (evil-set-initial-state 'fundamental-mode 'normal)

    ;; set indentation shift width
    (setq evil-shift-width 2))



  (use-package evil-commentary
    :ensure t
    :diminish t
    :config (evil-commentary-mode))

#+end_src
* stage 0 
** Org-mode configuration
*** general
#+begin_src emacs-lisp
    (defconst noir/ops-directory (expand-file-name "~/ops/")
      "Root directory for operational Org files.")

    (defconst noir/knw-directory (expand-file-name "~/knw/")
      "Root directory for knowledge files / Org-roam graph.")

    (use-package org
      :ensure t
      :hook
      (org-mode . org-indent-mode)
      (org-mode . flyspell-mode)
      :config
      (setq org-directory noir/ops-directory)
      (setq org-startup-folded 'overview)
      (setq org-agenda-files
            (list
             (expand-file-name "inbox" noir/ops-directory)
             (expand-file-name "agenda" noir/ops-directory)
             (expand-file-name "planner/weekly" noir/ops-directory)
             (expand-file-name "projects/active" noir/ops-directory)
             (expand-file-name "backlog" noir/ops-directory)))
      (global-set-key (kbd "C-c c") #'org-capture)
      (global-set-key (kbd "C-c a") #'org-agenda))

#+end_src
*** org-babel
#+begin_src emacs-lisp
(with-eval-after-load 'org
  (org-babel-do-load-languages
   'org-babel-load-languages
   '((js      . t)
     (python  . t)
     (haskell . t)
     (java    . t))))
#+end_src
*** org-roam
#+begin_src emacs-lisp
  (use-package org-roam
    :ensure t
    :demand t
    :custom
    (org-roam-directory noir/knw-directory)
    (org-roam-completion-everywhere t)
    :config
    (org-roam-db-autosync-mode)
    :bind (("C-c n l" . org-roam-buffer-toggle)
           ("C-c n f" . org-roam-node-find)
           ("C-c n i" . org-roam-node-insert)
           ("C-c n c" . org-roam-capture)))
#+end_src
*** org-roam-ui
#+begin_src emacs-lisp
  (use-package org-roam-ui
    :ensure t
    :after org-roam
    :commands (org-roam-ui-mode org-roam-ui-open)
    :config
    (setq org-roam-ui-sync-theme t
          org-roam-ui-follow t
          org-roam-ui-update-on-save t
          org-roam-ui-open-on-start nil))

  (global-set-key (kbd "C-c n u") #'org-roam-ui-open)
#+end_src
*** capture templates
#+begin_src emacs-lisp
  (setq org-roam-capture-templates
        '(("c" "Concept" plain
           "* Definition\n\n%?\n\n* Explanation\n\n* Related\n"
           :target (file+head "concepts/${slug}.org"
                              "#+title: ${title}\n#+filetags: :concept:\n")
           :unnarrowed t)

          ("o" "Topic" plain
           "* Overview\n\n%?\n\n* Concepts\n\n* Sources\n\n* Open Questions\n"
           :target (file+head "topics/${slug}.org"
                              "#+title: ${title}\n#+filetags: :topic:\n")
           :unnarrowed t)

          ("s" "Source" plain
           "* Summary\n\n%?\n\n* Key Ideas\n\n* Quotations\n\n* Connections\n\n* Bibliographic Data\n:PROPERTIES:\n:CITEKEY: ${citar-citekey}\n:AUTHOR: ${citar-author}\n:DATE: ${citar-date}\n:TYPE: ${citar-type}\n:PAGES: ${citar-pages}\n:END:\n"
           :target (file+head "sources/${citar-citekey}.org"
                              "#+title: ${note-title}\n#+filetags: :source:\n")
           :unnarrowed t)

          ("e" "Person" plain
           "* Who\n\n%?\n\n* Works\n\n* Ideas\n\n* Related\n"
           :target (file+head "people/${slug}.org"
                              "#+title: ${title}\n#+filetags: :person:\n")
           :unnarrowed t)

          ("r" "Project Thinking" plain
           "* Problem\n\n%?\n\n* Design\n\n* Insights\n\n* Related\n"
           :target (file+head "projects/${slug}.org"
                              "#+title: ${title}\n#+filetags: :project:\n")
           :unnarrowed t)

          ("x" "Index" plain
           "* Overview\n\n%?\n\n* Important Concepts\n\n* Key Sources\n\n* Open Questions\n"
           :target (file+head "indexes/${slug}.org"
                              "#+title: ${title}\n#+filetags: :index:\n")
           :unnarrowed t)))

  (setq org-capture-templates
        `(("i" "Inbox item" entry
           (file ,(expand-file-name "inbox/capture.org" noir/ops-directory))
           "* %?\nCaptured: %U\n")

          ("f" "Fleeting note" entry
           (file ,(expand-file-name "inbox/fleeting.org" noir/ops-directory))
           "* %?\nCaptured: %U\n")

          ("t" "Task" entry
           (file ,(expand-file-name "agenda/inbox.org" noir/ops-directory))
           "* TODO %?\nCaptured: %U\n")

          ("b" "Backlog idea" entry
           (file ,(expand-file-name "backlog/someday.org" noir/ops-directory))
           "* %?\nAdded: %U\n")

          ("p" "Project note" entry
           (file ,(expand-file-name "projects/active/inbox.org" noir/ops-directory))
           "* %^{Project Title}\n\n** Objective\n%?\n\n** Actions\n- [ ] \n\n** Insights\n- \n\n** Constraints\n- \n\n** Questions\n- \n")))
#+end_src
*** attachments setup
#+begin_src emacs-lisp
  (setq org-attach-id-dir (expand-file-name "attachments/" noir/knw-directory))

  (defun narmer/org-attach-setup ()
    "Ensure new roam notes have ID and attachment dir."
    (org-id-get-create))

  (add-hook 'org-roam-capture-new-node-hook #'narmer/org-attach-setup)
#+end_src
*** org-roam hook
#+begin_src emacs-lisp
  (defun narmer/org-roam-ensure-id ()
  "Ensure every Org-roam file has an ID."
  (when (and (derived-mode-p 'org-mode)
             (string-prefix-p (expand-file-name noir/knw-directory)
                              (or (buffer-file-name) "")))
    (org-id-get-create)))

(add-hook 'find-file-hook #'narmer/org-roam-ensure-id)
#+end_src
** mu4e configuration
#+begin_src emacs-lisp
  (use-package mu4e
  :ensure nil ;; part of mu installation
  ;; :load-path "/run/current-system/sw/share/emacs/site-lisp/elpa/mu4e-1.12.13/"
  ;; :load-path "/usr/share/emacs/site-lisp/mu4e/" ;; adjust based on your OS/distribution
  :defer 20
  :init (require 'mu4e)
  :config
  (setq
      ;; Basic maildir
      mu4e-maildir "~/mail/gmail"

      ;; Gmail folders (note: Gmail uses labels, which mbsync maps to folders)
      mu4e-sent-folder "/gmail/Sent Mail"
      mu4e-drafts-folder "gmail/Drafts"
      mu4e-trash-folder "/gmail/Trash"
      mu4e-refile-folder "/gmail/All Mail"

      ;; Behavior
      mu4e-get-mail-command "mbsync -a" ;; fetch mail via mbsync
      mu4e-update-interval 300          ;; refresh every 5 minutes
      mu4e-compose-signature-auto-include nil
      mu4e-change-filenames-when-moving t ;; needed for mbsync

      ;; Bookmarks
      mu4e-bookmarks
      '((:name "Unread messages" :query "flag:unread AND NOT flag:trashed" :key ?u)
      (:name "Today's messages" :query "date:today..now" :key ?t)
      (:name "Last 7 days" :query "date:7d..now" :key ?w)
      (:name "Messages with attachments" :query "mime:application/*" :key ?a))

      ;; Use smtpmail for sending
      ;; message-send-mail-function 'smtpmail-send-it
      smtpmail-smtp-server "smtp.gmail.com"
      smtpmail-smtp-service 587
      smtpmail-stream-type 'starttls

      ;; From address
      user-mail-address "narmernbazile@gmail.com"
      user-full-name "Narmer Noir Bazile"

      ;; Better headers
      mu4e-headers-date-format "%Y-%m-%d"
      mu4e-headers-time-format "%H:%M"
      mu4e-headers-fields
      '((:date . 25)
      (:flags . 6)
      (:from . 22)
      (:subject)))

  ;; HTML viewing
  (setq mu4e-view-prefer-html t
          mu4e-html2text-command "w3m -T text/html")

  ;; Org-mode integration
  ;; (require 'org-mu4e)

  ;; Allow inline image viewing
  (setq mu4e-view-show-images t
          mu4e-view-image-max-width 800))

  (defun my-smtpmail-send-it-with-gpg ()
  "Decrypt Gmail App Password via GPG and send email."
  (let ((password (string-trim
                  (shell-command-to-string
                      "gpg --quiet --for-your-eyes-only --no-tty -d ~/.mail/pass.gpg"))))
      (setq smtpmail-auth-credentials
          (list (list "smtp.gmail.com" 587 "narmernbazile@gmail.com" password)))
      (smtpmail-send-it)))

  (setq message-send-mail-function 'my-smtpmail-send-it-with-gpg)
  #+end_src
** calendar configuration 
#+begin_src emacs-lisp
  ;; (use-package calfw
  ;;   :ensure t
  ;;   :after org)
  ;; (use-package calfw-org
  ;;   :ensure t
  ;;   :after calfw
  ;;   :config
  ;;   (setq cfw:org-agenda-views '(("Week" . 7))))

  ;; ;; Load encrypted JSON credentials
  ;; (defun my/load-org-gcal-creds ()
  ;;   "Decrypt and parse org-gcal JSON credentials. Returns a hash table."
  ;;   (let* ((json-str (shell-command-to-string
  ;;                     "gpg --quiet --for-your-eyes-only --no-tty -d ~/.config/emacs/secrets/org-gcal-credentials.gpg"))
  ;;          (json-object-type 'hash-table)
  ;;          (json-array-type 'list)
  ;;          (json-key-type 'string))
  ;;     (json-read-from-string json-str)))

  ;; ;; Load credentials and set org-gcal variables BEFORE org-gcal is loaded
  ;; (let* ((creds (my/load-org-gcal-creds))
  ;;        (installed (gethash "installed" creds)))
  ;;   (setq org-gcal-client-id (gethash "client_id" installed)
  ;;         org-gcal-client-secret (gethash "client_secret" installed)
  ;;         org-gcal-file-alist
  ;;         '(("narmernbazile@gmail.com" . "~/org/gcal.org"))))

  ;; ;; Load org-gcal
  ;; (use-package org-gcal
  ;;   :ensure t
  ;;   :after org)
  ;;   ;; :config
  ;;   ;; Optional: automatic sync every 30 minutes
  ;;   ;;(run-at-time "00:00" (* 30 60) #'org-gcal-sync))
#+end_src
** cryptography 
#+begin_src emacs-lisp
  (use-package epa
    :ensure nil
    :config
    (setq epa-file-select-keys nil)
    (setq epa-file-encrypt-to '("narmernbazile@gmail.com"))
    (setq epg-pinentry-mode 'loopback)
    (epa-file-enable))
#+end_src
* stage 1 
** Project and File Management 
#+begin_src emacs-lisp
  (use-package projectile
    :ensure t
    :diminish projectile-mode
    :config
    (projectile-mode +1)
    (setq projectile-project-search-path '("~/src/"))
    (setq projectile-completion-system 'default)
    :bind-keymap
    ("C-c p" . projectile-command-map))
#+end_src
** Code Navigation and Search 
#+begin_src emacs-lisp
  (use-package consult
    :ensure t
    :bind (("C-s" . consult-line)
           ("C-c f" . consult-ripgrep)
           ("C-x b" . consult-buffer)
           ("M-y" . consult-yank-pop)))

  (use-package embark
    :ensure t
    :bind (("C-." . embark-act)
           ("C-;" . embark-dwim))
    :config
    (setq prefix-help-command #'embark-prefix-help-command))

  (use-package embark-consult
    :ensure t
    :after (embark consult)
    :hook (embark-collect-mode . consult-preview-at-point-mode))
#+end_src
** Linting, Formatting, and Diagnostics
#+begin_src emacs-lisp
  (use-package flycheck
    :ensure t
    :init (global-flycheck-mode))

  (use-package apheleia
    :ensure t
    :config
    (apheleia-global-mode +1))
#+end_src
** Debugging
#+begin_src emacs-lisp
  (use-package dap-mode
    :ensure t
    :after eglot
    :config
    (dap-auto-configure-mode)
    (ignore-errors (require 'dap-python))
    (ignore-errors (require 'dap-gdb-lldb))
    (ignore-errors (require 'dap-java)))

  ;; You may also wish to install:
  ;; debugpy for Python (pip install debugpy)
  ;; vscode-cpptools or lldb-vscode for C/C++
  ;; jdtls for Java (usually provided with your LSP)
#+end_src
** Version control
#+begin_src emacs-lisp
  (use-package magit
    :ensure t
    :bind (("C-x g" . magit-status)))

  (use-package diff-hl
    :ensure t
    :hook ((prog-mode . diff-hl-mode)
           (magit-post-refresh . diff-hl-magit-post-refresh)))
#+end_src
** Language configuration
*** C/C++
#+begin_src emacs-lisp
  (use-package c-ts-mode
    :ensure nil
    :hook ((c-mode c++-mode c-ts-mode c++-ts-mode) . eglot-ensure)
    :config
    (setq c-basic-offset 2))
  ;; Install clangd on your system (sudo apt install clangd or equivalent).
#+end_src
*** Python
#+begin_src emacs-lisp
  (use-package python
    :ensure nil
    :hook (python-ts-mode . (lambda ()
                              (setq python-indent-offset 2)
                              (eglot-ensure)))
    :config
    (setq python-shell-interpreter "python3"))

  (use-package blacken
    :ensure t
    :hook (python-mode . blacken-mode))
#+end_src
*** Java
#+begin_src emacs-lisp
  (use-package gradle-mode :ensure t)
#+end_src
*** JavaScript / Node.js
#+begin_src emacs-lisp
  ;; ------------------------------
  ;; JavaScript / Node.js Development
  ;; ------------------------------

  (use-package js-ts-mode
    :ensure nil
    :mode "\\.js\\'")

  ;; Use js-ts-mode for all JS files (already remapped in major-mode-remap-alist)
  (add-to-list 'auto-mode-alist '("\\.m?js\\'" . js-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.cjs\\'"  . js-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.jsx\\'"  . js-ts-mode))

  ;; Prefer tsserver for JS/TS LSP support in Eglot
  (with-eval-after-load 'eglot
    (add-to-list 'eglot-server-programs
                 '((js-ts-mode tsx-ts-mode typescript-ts-mode)
                   . ("typescript-language-server" "--stdio"))))

  ;; Basic indentation and formatting behaviour
  (setq js-indent-level 2)
  (setq typescript-indent-level 2)

  ;; NPM scripts helper
  (use-package npm-mode
    :ensure t
    :hook ((js-ts-mode . npm-mode)
           (typescript-ts-mode . npm-mode)))

  ;; Add helpful CAPE backends for JS
  ;; (file and dabbrev are already loaded; avoid duplicates)
  (add-to-list 'completion-at-point-functions #'cape-keyword)
#+end_src

*** Rust
#+begin_src emacs-lisp
  (use-package rust-mode :ensure t)
  (use-package rustic
    :ensure t
    :config
    ;; Use eglot instead of lsp-mode
    (add-hook 'rustic-mode-hook #'eglot-ensure)

    ;; Format on save
    ;; (setq rustic-format-on-save t)
    (setq rustic-indent-offset 2)
    ;; Optional: improve shell integration for Cargo commands
    (setq rustic-lsp-client 'eglot))
#+end_src
*** Nix
#+begin_src emacs-lisp
  (use-package nix-mode
  :ensure t
  :mode ("\\.nix\\'" . nix-mode))
#+end_src
*** Golang
#+begin_src emacs-lisp
  (use-package go-mode
    :ensure t
    :mode "\\.go\\'")

  (use-package go-ts-mode
    :ensure nil
    :config
  (setq go-ts-mode-indent-offset 2))

  (when (and (fboundp 'treesit-available-p) (treesit-available-p))
    (add-to-list 'major-mode-remap-alist '(go-mode . go-ts-mode)))

  (with-eval-after-load 'eglot
    ;; Ensure eglot knows how to start gopls for Go buffers.
    ;; Usually automatic, but explicit is robust.
    (add-to-list 'eglot-server-programs
                 '((go-mode go-ts-mode) . ("gopls"))))

  (defun narmer/go-before-save ()
    "Format Go buffers deterministically before saving."
    ;; If you prefer goimports (imports + formatting), keep gofmt off and use goimports via Apheleia (see below),
    ;; OR run it here if you prefer. This function is a conservative default.
    (when (derived-mode-p 'go-mode 'go-ts-mode)
      (eglot-format-buffer)))

  (use-package eglot
    :ensure nil
    :hook ((go-mode . eglot-ensure)
           (go-ts-mode . eglot-ensure))
    :config
    ;; gopls can be chatty; this keeps edits responsive.
    (setq eglot-send-changes-idle-time 0.3))

  ;; Format on save (via eglot formatting).
  (add-hook 'before-save-hook #'narmer/go-before-save)

  (defun narmer/go-two-column-tabs ()
    (setq-local indent-tabs-mode t)
    (setq-local tab-width 2))

  (add-hook 'go-mode-hook #'narmer/go-two-column-tabs)
  (add-hook 'go-ts-mode-hook #'narmer/go-two-column-tabs)

  (defun narmer/go-disable-apheleia ()
    (when (boundp 'apheleia-mode)
      (apheleia-mode -1)))

  (add-hook 'go-mode-hook #'narmer/go-disable-apheleia)
  (add-hook 'go-ts-mode-hook #'narmer/go-disable-apheleia)
#+end_src
** Terminal and Shell Integration
#+begin_src emacs-lisp
  (use-package vterm
    :ensure t
    :commands vterm)

  ;; You can bind a shortcut (e.g., C-c t) to open a terminal in the project root. 
#+end_src
** UI and QoL Enhancements
#+begin_src emacs-lisp
  (use-package treemacs
    :ensure t
    :bind (("C-x t t" . treemacs))
    :config
    (setq treemacs-is-never-other-window t))

  (use-package breadcrumb
    :ensure t
    :hook (prog-mode . breadcrumb-mode))

  (use-package undo-tree
    :ensure t
    :init
    (global-undo-tree-mode))
#+end_src
** Snippets and Templates
#+begin_src emacs-lisp
  (use-package yasnippet
    :ensure t
    :config
    (yas-global-mode 1))

  (use-package yasnippet-snippets
    :ensure t)
#+end_src
** Performance Optimizations
#+begin_src emacs-lisp
  (setq read-process-output-max (* 4 1024 1024)) ;; 4MB
  (setq gc-cons-threshold (* 100 1024 1024))
#+end_src
** Global Custom Keybindings
#+begin_src emacs-lisp
  ;; -------------------------------------------------------------------
  ;; Global Custom Keybindings
  ;; -------------------------------------------------------------------
  ;; These bindings provide rapid access to frequently used development
  ;; tools and commands across all major modes.  They do not conflict
  ;; with standard Emacs conventions (`C-c` followed by a lowercase key
  ;; is reserved for user use).

  ;; Open an integrated terminal (vterm)
  (global-set-key (kbd "C-c t") #'vterm)

  ;; Launch Magit status interface for Git version control
  (global-set-key (kbd "C-c g") #'magit-status)

  ;; Search the current project or directory using Consult + Ripgrep
  (global-set-key (kbd "C-c s") #'consult-ripgrep)

  ;; Rename symbol under cursor via Eglot LSP interface
  (global-set-key (kbd "C-c i") #'eglot-rename)

  ;; Switch buffers using Consult’s enhanced completion
  ;;(global-set-key (kbd "C-c b") #'consult-buffer)

  ;; Start a debugging session using DAP (Debug Adapter Protocol)
  (global-set-key (kbd "C-c d") #'dap-debug)

  ;; Optional: Quick access to file finder (consult-find or project-find-file)
  (global-set-key (kbd "C-c f") #'consult-find)

  ;; Optional: Toggle file tree / sidebar if using Treemacs or Neotree
  (global-set-key (kbd "C-c e") #'treemacs) ;; uncomment if applicable

#+end_src 
* stage 2
** citar-org-roam
#+begin_src emacs-lisp
  (use-package citar-org-roam
    :ensure t
    :after (citar org-roam)
    :demand t
    :custom
    (citar-org-roam-capture-template-key "s")
    :config
    (citar-org-roam-mode)
    :bind (("C-c n b" . citar-open-notes)))

#+end_src
** Citation Management (interop with Zotero)
#+begin_src emacs-lisp
  (use-package citar
    :ensure t
    :demand t
    :custom
    ;; the core files for your citation workflow
    (citar-bibliography '("~/lib/library.bib"))

    ;; where Citar should search for PDFs
    (citar-library-paths '("~/lib/"))

    ;; tells Citar how to find local files from Zotero
    (citar-file-open-functions
     '((t . find-file))) ;; you may later upgrade this to your PDF reader of choice

    ;; ensures completion sources work everywhere
    (citar-templates
     '((note . "* ${title}\n:PROPERTIES:\n:Author: ${author}\n:Year: ${year}\n:Custom_ID: ${=key=}\n:END:\n\n")
       (main . "${author editor} (${year}) · *${title}*")
       (suffix . "  ${=key=}")
       (preview . "${author editor}, *${title}*, ${year}.")))

    :bind (("C-c b b" . citar-open)
           ("C-c b o" . org-open-at-point)
           ("C-c b c" . citar-insert-citation)
           ("C-c b r" . citar-open-ref)
           ("C-c b n" . citar-open-notes)))


#+end_src
** Org-cite Integration
#+begin_src emacs-lisp
  (use-package oc
    :after org
    :config
    (setq org-cite-insert-processor 'citar
          org-cite-follow-processor 'citar
          org-cite-activate-processor 'citar))
#+end_src
* stage 3
** Nix Integration
#+begin_src emacs-lisp
;; Project-scoped env for GUI Emacs via direnv (recommended for Nix devShells)

(use-package direnv
  :ensure t
  :config
  (direnv-mode))

;; If you use eglot, ensure it discovers tools from the direnv-updated PATH.
;; Usually automatic once direnv-mode is enabled.
#+end_src
** AI enablement
#+begin_src emacs-lisp
  (use-package gptel
  :ensure t
  :config
  (setq gptel-backend
        (gptel-make-openai
         "ollama"
         :host "localhost:11434"
         :protocol "http"
         :endpoint "/v1/chat/completions"
         :stream t
         :models '("mistral" "llama3:8b" "llama2:13b" "codellama")
         :key nil))

  (add-hook 'gptel-post-response-functions
            (lambda (beg end)
              (when 't
                (goto-char end)
                (forward-line 4))))
  (setq gptel-use-curl t)
  (setq gptel-model 'llama3:8b)
  (add-to-list 'display-buffer-alist ;; window ergonomics
        '("\\*ollama\\*"
  	(display-buffer-in-side-window)
  	(side . right)
  	(window-width . 0.4))))
#+end_src
